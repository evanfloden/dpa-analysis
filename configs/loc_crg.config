process {
    executor = 'crg'
    queue = 'long-sl7'
    scratch = true
    time = '12h'
    memory = { 24.GB * task.attempt }
    errorStrategy = { task.attempt < 3 ? 'retry' : 'finish' } 
    maxRetries = 4
}

singularity {
    enabled = false
}

docker {
    enabled = true
}
