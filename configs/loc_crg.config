process {
    executor = 'crg'
    queue = 'cn-el7'
    scratch = true
    time = '24h'
    memory = { 10.GB * task.attempt }
    errorStrategy = { task.attempt < 5 ? 'retry' : 'finish' } 
    maxRetries = 5
}

singularity {
    enabled = false
}

docker {
    enabled = true
}
